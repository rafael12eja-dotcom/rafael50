// prisma/schema.prisma

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"              // simples para começar
  url      = "file:./dev.db"       // não precisa de .env agora
}

// Quando quiser, adicionamos os models aqui.
// Exemplo futuro:
// model Receita {
//   id        Int      @id @default(autoincrement())
//   titulo    String
//   valor     Decimal  @db.Decimal(10,2)
//   criadoEm  DateTime @default(now())
// }
